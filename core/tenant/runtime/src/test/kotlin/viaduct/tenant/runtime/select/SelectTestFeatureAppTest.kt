package viaduct.tenant.runtime.select

/**
 * Stub that triggers Bazel codegen. The real schema is on the
 * [viaduct.tenant.runtime.executioncontext.ExecutionContextTestSchema] object
 * in testFixtures via @TestSchema.
 */
abstract class SelectTestFeatureAppTest
